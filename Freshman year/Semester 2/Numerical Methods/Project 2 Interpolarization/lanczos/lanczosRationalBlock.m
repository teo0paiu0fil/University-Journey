function [V , W] = lanczosRationalBlock(A,B,C)

  [n m] = size(A);

  S0 = inv(A - a(1,1)*eye(n));
  R0 = S0'*C';
  
  V(1,1,) = S0 * inv(H1
  
  V(1,1) = abs(V(1,1));  
  W(1,1) = abs(W(1,1));
  

  
  for i = 1:
  



  %% nu am reusit sa o duc la capat

endfunction